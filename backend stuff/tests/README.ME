You must cd all the way into the "tests" folder

To run, type in the terminal "go test -v"

If a bunch of weird lines pop up, please inform Don

All test files must end with "_test.go"

All functions names of the tests must start with "Test"

For all unit test functions, run the function "initializeTestDatabase()" at the beginning